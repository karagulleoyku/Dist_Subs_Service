syntax = "proto3";

message Capacity {
    int32 server_id = 1;      // Values: 1, 2, or 3
    int32 server_status = 2;  // Current subscriber count
    int64 timestamp = 3;      // UNIX epoch time
}
